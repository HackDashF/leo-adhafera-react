import CircularSpinner from "../../components/CircularSpinner";

const Lists = () => {
  return (
    <div>
      <h2>My Lists</h2>
      {/* lists from server */}
      <CircularSpinner />
    </div>
  );
};

export default Lists;
